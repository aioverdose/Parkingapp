"use client";
import { WandSparkles } from "lucide-react";
import type { AspectRatio } from "@/lib/video/types";

export default function VideoPromptPanel({ title, prompt, aspectRatio, durationSeconds, onChange, onGenerate, loading }: { title: string; prompt: string; aspectRatio: AspectRatio; durationSeconds: number; onChange: (value: Partial<{ title: string; prompt: string; aspectRatio: AspectRatio; durationSeconds: number }>) => void; onGenerate: () => void; loading: boolean }) {
  return <div className="rounded-2xl border border-zinc-200 dark:border-zinc-800 bg-white dark:bg-zinc-900 p-5 space-y-4"><div><h2 className="font-bold">Describe your video</h2><p className="text-xs text-zinc-500 mt-1">The fallback generator creates a complete parking-app demo when no LLM is configured.</p></div><label className="block text-xs font-medium text-zinc-500">Project name<input value={title} onChange={(e) => onChange({ title: e.target.value })} className="mt-1 w-full rounded-xl border border-zinc-200 dark:border-zinc-700 bg-transparent px-3 py-2 text-sm" /></label><label className="block text-xs font-medium text-zinc-500">Prompt<textarea value={prompt} onChange={(e) => onChange({ prompt: e.target.value })} rows={4} placeholder="Create a 45-second promotional demonstration..." className="mt-1 w-full rounded-xl border border-zinc-200 dark:border-zinc-700 bg-transparent px-3 py-2 text-sm resize-y" /></label><div className="grid grid-cols-2 gap-3"><label className="text-xs font-medium text-zinc-500">Format<select value={aspectRatio} onChange={(e) => onChange({ aspectRatio: e.target.value as AspectRatio })} className="mt-1 w-full rounded-xl border border-zinc-200 dark:border-zinc-700 bg-transparent px-2 py-2 text-sm"><option value="9:16">9:16 vertical</option><option value="16:9">16:9 landscape</option><option value="1:1">1:1 square</option></select></label><label className="text-xs font-medium text-zinc-500">Duration<select value={durationSeconds} onChange={(e) => onChange({ durationSeconds: Number(e.target.value) })} className="mt-1 w-full rounded-xl border border-zinc-200 dark:border-zinc-700 bg-transparent px-2 py-2 text-sm">{[15, 30, 45, 60].map((value) => <option key={value} value={value}>{value} seconds</option>)}</select></label></div><button onClick={onGenerate} disabled={loading || !prompt.trim()} className="w-full flex justify-center items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50"><WandSparkles size={16} />{loading ? "Generating storyboard..." : "Generate Storyboard"}</button></div>;
}
