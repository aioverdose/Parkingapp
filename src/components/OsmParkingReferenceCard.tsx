import { OsmAttribution } from "./OsmAttribution";
export function OsmParkingReferenceCard({ record }: { record: { name: string; address_area?: string; hours?: string; pricing?: string; restrictions?: string; source_url: string; source_name?: string } }) { return <article className="rounded-2xl border border-blue-100 bg-blue-50/50 p-4"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold text-blue-950">{record.name}</h3><span className="rounded-full bg-blue-100 px-2 py-1 text-[10px] font-bold uppercase tracking-wide text-blue-800">Reference</span></div>{record.address_area && <p className="mt-2 text-sm text-zinc-700">{record.address_area}</p>}{record.hours && <p className="mt-1 text-sm">Hours: {record.hours}</p>}{record.pricing && <p className="mt-1 text-sm">{record.pricing}</p>}<p className="mt-2 text-xs text-zinc-600">{record.restrictions || "Verify signage and local rules before parking."}</p><a className="mt-3 inline-block text-xs font-semibold text-blue-700 underline" href={record.source_url} target="_blank" rel="noreferrer">View source: {record.source_name || "OpenStreetMap"}</a><div className="mt-3"><OsmAttribution /></div></article>; }
